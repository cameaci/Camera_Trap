# Dependencies
/node_modules

# Production build
/build

# Generated Docusaurus files
/.docusaurus
/.cache-loader

# Generated at build time from the repo root models.json by
# scripts/sync-models.mjs. Never committed, to avoid drift from the
# real catalog.
/src/data/models.json

# Generated at build time from docs/ by scripts/sync-pages.mjs: the
# sections and pages the home page lists. Never committed, so the home
# page cannot drift from the sidebar.
/src/data/pages.json

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
