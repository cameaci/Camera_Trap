# Alembic configuration file

[alembic]
# Path to migration scripts
script_location = alembic

# Template used to generate migration file names
file_template = %%(year)d%%(month).2d%%(day).2d_%%(hour).2d%%(minute).2d_%%(rev)s_%%(slug)s

# Timezone to use when rendering the date within the migration file
# as well as the filename. Default: UTC
# timezone = UTC

# Maximum length of characters to apply to the
# "slug" field
truncate_slug_length = 40

# Set to 'true' to run the environment during
# the 'revision' command, regardless of autogenerate
# revision_environment = false

# Set to 'true' to allow .pyc and .pyo files without
# a source .py file to be detected as revisions in the
# versions/ directory
# sourceless = false

# Note: version_locations intentionally not set. Alembic splits its
# value on whitespace (_split_on_space_comma_path), which breaks paths
# containing spaces such as the Windows install root
# C:\Program Files\AddaxAI\resources\backend\_internal. Without this
# key alembic falls back to script_location/versions, which we set
# programmatically in app/db/migrations.py to an absolute path that
# does not pass through the splitter.

# Output encoding used when revision files
# are written from script.py.mako
# output_encoding = utf-8

# SQLAlchemy database URL - set via env var in env.py
sqlalchemy.url =

[post_write_hooks]
# Post-write hooks for code formatters (ruff format)
hooks = ruff
ruff.type = console_scripts
ruff.entrypoint = ruff
ruff.options = format REVISION_SCRIPT_FILENAME

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
